# Container runtime dependencies for TinyGPT.
# PyTorch is pinned to the official CPU wheel index so Docker builds do not
# pull the large CUDA-enabled PyPI distribution.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.7.1+cpu
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
redis>=5.0.0
numpy>=1.26.0
sqlalchemy>=2.0.30
asyncpg>=0.29.0
aiosqlite>=0.20.0
prometheus-client>=0.20.0
